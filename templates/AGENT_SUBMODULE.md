# AI Agent Context: {{MODULE_NAME}} (Submodule)

This directory is a **submodule** of the parent monorepo. For repo-wide protocol, workflow scripts, and JIT context, use the **repository root**.

- **Root agent file:** Read [GEMINI.md](../GEMINI.md) or [CLAUDE.md](../CLAUDE.md) at the repo root for full protocol.
- **Context command:** From repo root run: `./scripts/context.py fetch <key>` (or `uv run scripts/context.py fetch <key>`). List keys with `./scripts/context.py list`.

## This module

**{{MODULE_NAME}}:** {{ONE_LINE_DESCRIPTION}}

*(Agent: Add key entry points, commands, and lessons learned below when working in this module.)*
